// MQTT publishing code has been merged into sketch_may7a.ino.
// This older dummy publisher is intentionally disabled to avoid duplicate
// setup() and loop() definitions when compiling the IoT sketch folder.
